---
version: 2
plan: !include builds/plan.yaml
stages: !include builds/stages.yaml
Build: !include builds/artifact.yaml
branch-overrides:
  -
    integration-branch:
      branch-config:
      disable-expiry: true
      Build: !include builds/artifact.yaml

