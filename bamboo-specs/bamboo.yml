---
version: 2
plan:
  project-key: TEST
  key: TEST
  name: TESTN
branch-overrides:
-
develop:
branch-config:
disable-expiry: true
-
^integration\/[A-Za-z\d]+$:
branch-config:
disable-expiry: true
Build: !include builds/artifact.yaml
-
main:
Build: !include builds/artifact.yaml
