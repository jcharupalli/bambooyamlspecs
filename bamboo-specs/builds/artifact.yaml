  key: JOB1
  tasks:
  - script:
      interpreter: SHELL
      scripts:
      - echo "hiiiiiii" >> hi.txt
      description: echo
  artifacts:
  - name: deploy1
    pattern: '*.txt'
    shared: true
    required: true
